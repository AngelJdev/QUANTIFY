<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network: Direct WiFi connection to QUANTIFY backend -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Sensors: Heart rate & stress telemetry (Module 4.2) -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <!-- Vibration: Haptic feedback on habit completion (Screen 3.3) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Wake lock: Keep sync engine running -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Declare this is a Wear OS standalone app (no phone companion) -->
    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:name=".QuantifyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault"
        android:usesCleartextTraffic="true">

        <!-- Standalone Wear OS app (NO phone app required) -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
